name: Build LifeXP icon pack

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'Create the full visual catalog or build a reviewed pack'
        required: true
        type: choice
        options:
          - catalog
          - build
        default: catalog
      source_zip:
        description: 'Repository path to the original icon ZIP'
        required: true
        type: string
        default: assets/source/game-icons/game-icons.net.svg.zip
      selection_manifest:
        description: 'Repository path to the reviewed selection manifest'
        required: false
        type: string
        default: assets/icon-pack/selection.json

permissions:
  contents: read

jobs:
  build:
    name: Validate and package icons
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Create full visual catalog
        if: inputs.mode == 'catalog'
        env:
          SOURCE_ZIP: ${{ inputs.source_zip }}
        run: |
          set -euo pipefail
          node tools/build_icon_catalog.js \
            --source "$SOURCE_ZIP" \
            --out dist/icon-catalog

      - name: Build reviewed icon pack
        if: inputs.mode == 'build'
        env:
          SOURCE_ZIP: ${{ inputs.source_zip }}
          SELECTION_MANIFEST: ${{ inputs.selection_manifest }}
        run: |
          set -euo pipefail
          node tools/build_icon_pack.js \
            --mode build \
            --source "$SOURCE_ZIP" \
            --manifest "$SELECTION_MANIFEST" \
            --out dist/icon-pack

      - name: Upload catalog or pack artifact
        uses: actions/upload-artifact@v4
        with:
          name: lifexp-icon-pack-${{ inputs.mode }}
          path: dist
          if-no-files-found: error
          retention-days: 14
